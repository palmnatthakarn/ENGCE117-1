#include <stdio.h>
char *GetText();
int main() {
  char *str ;
  str = GetText();
  printf("The passed Text is : %s", str);
  return 0;
}
char *GetText() {
  char *text = new char[16];
  printf ("Enter a Text :");
  gets(text);
  return text;
}
